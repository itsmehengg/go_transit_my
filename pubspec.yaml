name: go_transit_my
description: "Smart Public Transport Tracker"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  supabase_flutter: ^2.17.2
  image_picker: ^1.2.3
  http: ^1.6.0
  flutter_map: ^8.3.1
  latlong2: ^0.10.1
  gtfs_realtime_bindings: ^1.2.0
  archive: ^4.2.0
  shared_preferences: ^2.5.3
  geolocator: ^14.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
