# Data layer

`MalaysiaTransportApi` connects to Malaysia's official `api.data.gov.my` GTFS endpoints.

`TransportRepository` adds caching, nearby-stop distance calculations, and realtime vehicle filtering.

`LocationService` handles Android location permission and GPS access.

`UserPreferences` stores non-critical app preferences, favourites, and recent searches locally.
